{
  "name": "lesson-scheduling",
  "version": "1.0.0",
  "private": true,
  "description": "Convenience scripts for running the Lesson Scheduling PERN app locally without Docker.",
  "scripts": {
    "setup": "npm install && npm install --prefix server && npm install --prefix client",
    "install:server": "npm install --prefix server",
    "install:client": "npm install --prefix client",
    "migrate": "cd server && npm run migrate:up",
    "seed": "cd server && npm run seed",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently -k -n api,web -c blue,magenta \"npm run dev:server\" \"npm run dev:client\"",
    "build": "cd client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
